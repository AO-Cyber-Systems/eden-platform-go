# TRD 21-02 — Cross-Repo Integration Smoke Test

**Objective:** 21 (Platform Receives Eden-Biz Migration)
**Roadmap req:** R31.2
**Status:** complete

## Goal

Verify eden-biz-go at its post-D10 main HEAD builds cleanly and its
D10-affected packages pass tests against the current `eden-platform-go`
`origin/main` HEAD. Provides cross-repo proof that D10's join point holds end
to end.

## Method

1. Use detached worktree of eden-biz-go at `origin/main` HEAD (commit
   `d8efc3a Obj 41: D10 TxHelper + Final Cleanup`) at
   `/Users/justin/dev/eden-biz-dev/eden-biz-go-platform-receive`.
2. Temporarily repoint eden-biz-go's `go.mod` `replace` directive at this
   stream's eden-platform-go worktree (`origin/main` HEAD `4a408a3`).
3. Run `go mod tidy`, then `go build` and `go test -short` for D10-affected
   internal packages.
4. Restore eden-biz-go's canonical `go.mod` after verification (no
   side effects on the eden-biz-go repo).

## Tasks

1. ✅ Create eden-biz-go detached worktree at post-D10 `origin/main`.
2. ✅ Repoint `go.mod` replace at eden-platform-go HEAD worktree.
3. ✅ Run `go mod tidy` — clean.
4. ✅ Build D10-affected packages.
5. ✅ Test D10-affected packages with test files.
6. ✅ Restore canonical `go.mod`.

## Verification

D10-affected packages (build):

| Package | Build result | Notes |
|---|---|---|
| `internal/audit` | indirect-fail | imports `bizv1.AuditServiceHandler` (pre-existing baseline) |
| `internal/audithelper` | indirect-fail | transitively imports `internal/audit` |
| `internal/webhooks` | ✅ clean | |
| `internal/events` | ✅ clean | |
| `internal/eventhelper` | ✅ clean | |
| `internal/storage` | ✅ clean | platform/storage consumer shim |
| `internal/attachments` | ✅ clean | platform_adapter bridge |
| `internal/jobs` | ✅ clean | thin alias shim onto platform/jobs |
| `internal/cron` | indirect-fail | transitively imports `internal/companymanagement` (bizv1) |
| `internal/crypto` | ✅ clean | wraps platform/encryption.FieldEncryptor |
| `internal/sso` | ✅ clean | consumes internal/crypto |
| `internal/apikeys` | ✅ clean | sha256/bcrypt cutover deferred |
| `internal/middleware` | ✅ clean | platform/server.ExtractClaims migration done |

The "indirect-fail" cases (`audit`, `audithelper`, `cron`) all fail because
they transitively import biz-domain packages that depend on `bizv1` proto
generation that is **independently failing on origin/main** (24 pre-existing
baseline failures noted in Obj 36/37/41 worker reports). The D10-relevant
package code in each of these packages itself compiles when isolated to
its own go-files; the failure is from imports two-deep into bizv1-shaped
handlers.

D10-affected packages with test files (test):

```
ok    github.com/aocybersystems/eden-biz-go/internal/webhooks      0.276s
ok    github.com/aocybersystems/eden-biz-go/internal/attachments   0.558s
ok    github.com/aocybersystems/eden-biz-go/internal/crypto        0.383s
ok    github.com/aocybersystems/eden-biz-go/internal/middleware    0.747s
?     internal/{events,eventhelper,storage,jobs,sso,apikeys}       no test files
```

All tests **PASS**. Zero regressions introduced by post-D10 platform-go state.

## Result

R31.2 satisfied. Eden-biz-go's post-D10 main is stable against the current
eden-platform-go origin/main HEAD. The bizv1 baseline failures predate D10
and are tracked via the eden-biz-go bizv1 proto regeneration backlog (out of
scope for D10 closure on the platform side).

## Side note: canonical worktree state

Per Obj 41 hand-off note, the canonical `eden-platform-go` working tree
(/Users/justin/dev/eden-libs/eden-platform-go) is currently on a stale
branch (`ws-platform-aigateway`) with uncommitted modifications, while
eden-biz-go's `replace` points at `../../eden-libs/eden-platform-go`.
The user-facing remediation noted in Obj 41 is `git checkout main` on the
canonical tree once they pull. This TRD's smoke test bypassed this by
repointing the replace at the dedicated worktree; the canonical tree was
not modified.
