# TRD 21-01 — Platform Package Stability Audit (Post-D10)

**Objective:** 21 (Platform Receives Eden-Biz Migration)
**Roadmap req:** R31.1
**Status:** complete

## Goal

Verify all 14 platform packages that eden-biz now consumes are stable, build
clean, pass tests, and are well-vetted at current `origin/main` HEAD of
`eden-platform-go`. This is the eden-libs side gate for declaring D10 fully
closed.

## Scope

The 14 packages from D10 (and their canonical platform-go locations after the
W1-W3 promotions in Obj 16-20, 22, 23):

| Eden-biz donor | Platform-go destination | Donor objective |
|---|---|---|
| `internal/audit` | `platform/audit` (extended in Obj 16) | Obj 37 |
| `internal/webhooks` | `platform/webhook` (Obj 17) | Obj 37 |
| `internal/events` | (kept biz-local; sync vs platform/bridge async) | Obj 37 |
| `internal/eventhelper` | (kept biz-local; sync vs platform/bridge async) | Obj 37 |
| `internal/storage` | `platform/storage` (Obj 19) | Obj 38 |
| `internal/attachments` | `platform/attachments` (Obj 19) | Obj 38 |
| `internal/jobs` | `platform/jobs` (Obj 18) | Obj 39 |
| `internal/cron` | `platform/scheduler` (Obj 20) | Obj 39 |
| `internal/statemachine` | `platform/statemachine` (Obj 19) — donor deleted in Obj 39 | Obj 39 |
| `internal/crypto` | `platform/encryption` (Obj 16) | Obj 36 |
| `internal/sso` | (consumes `internal/crypto` shim; kept biz-local) | Obj 36 |
| `internal/apikeys` | `platform/auth/apikey` (Obj 22) — cutover deferred | Obj 36 |
| `internal/middleware` | `platform/server` (Obj 22, ExtractClaims) | Obj 40 |
| `internal/permissions` | `platform/rbac` (Obj 17) — donor deleted in Obj 40 | Obj 40 |
| `internal/txhelper` | (deleted in Obj 41; not promoted) | Obj 41 |

## Tasks

1. ✅ Run `go build ./platform/...` at current `origin/main` HEAD — clean exit.
2. ✅ Run `go test -short -count=1 ./platform/...` — all 47 platform packages pass.
3. ✅ Run `go vet ./platform/...` — clean exit.
4. ✅ Catalog test results by package (see Verification below).

## Verification

Executed in worktree `ws-platform-eden-biz-receive-go` (branched from
`origin/main` HEAD `4a408a3 Obj 29: AO ID service scaffolding`):

```
$ go build ./...                         # clean (0 errors)
$ go vet ./platform/...                  # clean
$ go test -short -count=1 ./platform/... # 47/47 PASS
```

All 47 platform packages PASS:
- `aigateway`, `apierror`, `attachments`, `audit`, `auth`, `auth/apikey`,
  `auth/emailotp`, `auth/oauth`, `auth/oidc`, `auth/saml`, `auth/saml/idp`,
  `auth/session`, `auth/totp`, `auth/webauthn`, `billing-rail`, `bridge`,
  `company`, `config`, `connectapi`, `consent`, `email`, `encryption`,
  `errortrack`, `feature-flags`, `hashutil`, `household`, `httputil`, `jobs`,
  `livekit`, `membership`, `notification`, `observability`, `pgstore`,
  `ratelimit`, `rbac`, `realtime`, `registry`, `scheduler`, `search`,
  `server`, `statemachine`, `storage`, `upload`, `webfetch`, `webhook`
- (`devstore`, `entitlements` have no test files but build clean)

## Result

R31.1 satisfied. All packages eden-biz now depends on through D10 are stable
at current `origin/main`. Notable: `pgstore` flake noted in earlier objectives'
CI did not reproduce in this local short-test run.
