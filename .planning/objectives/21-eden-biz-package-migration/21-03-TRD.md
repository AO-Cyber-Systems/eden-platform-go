# TRD 21-03 — Backlog Consolidation + CAPABILITIES.md Annotation

**Objective:** 21 (Platform Receives Eden-Biz Migration)
**Roadmap req:** R31.3, R31.4
**Status:** complete

## Goal

Confirm `eden-biz-go/PLATFORM_BACKLOG.md` (filed by Obj 41) covers all D10
deferrals and surface the eden-libs / platform-go side mapping. Annotate
`CAPABILITIES.md` so the consumer registry reflects post-D10 reality.

## Tasks

1. ✅ Read `PLATFORM_BACKLOG.md` (eden-biz-go).
2. ✅ Map each deferred item to its destination workstream and owner repo.
3. ✅ Annotate `CAPABILITIES.md` `eden-biz-dev` consumer entry with the
   post-D10 status.

## Backlog mapping (eden-libs / platform-go side)

| # | Backlog item | Status in BACKLOG | eden-libs / platform-go work needed | Destination |
|---|---|---|---|---|
| 1 | `apikeys` sha256→bcrypt + prefix cutover | DEFERRED | None on platform side; `platform/auth/apikey` is already complete (Obj 22). Cutover is **destructive customer-comm** done in eden-biz-go + ops. | Future `ws-ops-apikeys-cutover` (eden-biz-dev + ops repos). |
| 2 | `audit/audithelper/webhooks/events/eventhelper` Connect upstreaming | DEFERRED | Could grow `platform/audit` + `platform/webhook` to expose Connect surfaces. Gated on 2+ consumers needing the same shape. | Future `ws-platform-audit-connect` or similar (eden-libs). |
| 3 | `attachments` multi-tenancy upstream | DEFERRED | Grow `platform/attachments` to add CompanyID + RLS posture. Gated on `platform/company` having stabilized RLS. | Future `ws-platform-attachments-tenant` (eden-libs). |
| 4a | `cron` `Timeout` per task | DEFERRED | None; `platform/scheduler.ScheduleSpec.Timeout` already supported. eden-biz needs to plumb it on a per-task basis when SLOs emerge. | In-line, eden-biz-side. |
| 4b | Forward state machines (demo, license, saas, subscription) | DEFERRED | None; `platform/statemachine` ready. Owning features will import directly when built. | In-line with Obj 35 + future SaaS / subscription objectives. |
| 5 | `middleware` (CORS / IP rate-limit / basicauth / booking-slug / tenant) | DEFERRED | Grow `platform/server` extension package. Gated on 2+ consumers needing same middleware shape. | Future `ws-platform-server-extension` (eden-libs). |
| 6 | `txhelper` | DONE | None; package was deleted in Obj 41 (zero importers; not promoted). | — |

**Summary by destination:**
- eden-libs / platform-go future workstreams: 3 items (#2 audit-connect, #3 attachments-tenancy, #5 server-extension) — all gated on multi-consumer demand.
- eden-biz-side / ops: 3 items (#1 apikeys cutover, #4a/b inline use of existing platform features).
- Closed: 1 item (#6 txhelper).

## CAPABILITIES.md annotation

Updated `CAPABILITIES.md` §2 (Ecosystem Map) `Registered consumers` list so
the `eden-biz-dev` entry now reads:

> `eden-biz-dev` — pure platform consumer post-D10 (Obj 36-41 closed
> 2026-05-09; M3 milestone reached). Consumes
> `platform/{audit,webhook,storage,attachments,jobs,scheduler,statemachine,encryption,server,auth/apikey}`.
> Six follow-on items deferred and tracked in `eden-biz-go/PLATFORM_BACKLOG.md`.

Deeper section-6 schema-level updates to CAPABILITIES.md (the §6 layout still
predates Obj 16-29 promotions) are intentionally deferred to a separate doc
hygiene pass; out of scope for D10 closure.

## Result

R31.3 + R31.4 satisfied. Backlog mapping recorded; CAPABILITIES annotation
landed on the canonical eden-libs root copy.
